export class GoalHeader {
    constructor(
        public id: string,
        public title: string,
        public postCount: number,
        public updatedAt: string,
        public author: string,
    ) {
    }
}